class Project {
    constructor(title, icon, content, image, language, repository, screenshots, app) {
        this.title = title;
        this.icon = icon,
        this.content = content;
        this.image = image;
        this.language = language;
        this.repository = repository;
        this.screenshots = screenshots;
        this.app = app;
    }
}
export default Project;